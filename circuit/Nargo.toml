[package]
name = "prove_credential"
type = "bin"
authors = [""]
# Compiler version pinned by scripts/compile-circuit.sh; Noir's
# constraint parser rejects beta tags so we don't write one here.

[dependencies]
eddsa = { tag = "v0.1.0", git = "https://github.com/noir-lang/eddsa" }
